name: Guardian Quality Gates

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch: {}

env:
  NODE_VERSION: '18'

jobs:
  # Frontend quality checks
  frontend-quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: TypeScript type check
        run: npm run typecheck || npm run -w apps/web typecheck

      - name: ESLint code quality
        run: npm run lint || npm run -w apps/web lint

      - name: Build application
        run: npm run build || npm run -w apps/web build
        env:
          ANALYZE: true

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: .next
          retention-days: 1

  performance-tests:
    runs-on: ubuntu-latest
    needs: frontend-quality
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      - run: npm ci
      - uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: .next
      - run: npm install -g @lhci/cli@0.12.x
      - name: Build for production
        run: npm run build || npm run -w apps/web build
      - name: Start production server
        run: npm start & || npm run -w apps/web start &
        env:
          PORT: 3000
      - run: npx wait-on http://localhost:3000
      - run: lhci autorun
        env:
          LHCI_GITHUB_APP_TOKEN: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}

  bundle-analysis:
    if: false
    runs-on: ubuntu-latest
    needs: frontend-quality
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      - run: npm ci
      - name: Analyze bundle size
        run: npm run analyze || npm run -w apps/web analyze
        env:
          ANALYZE: true
      - uses: actions/upload-artifact@v4
        with:
          name: bundle-analysis
          path: .next/analyze
          retention-days: 7
      - if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            const statsPath = path.join('.next/analyze', 'bundle-analyzer-report.json');
            if (fs.existsSync(statsPath)) {
              const stats = JSON.parse(fs.readFileSync(statsPath, 'utf8'));
              const totalSize = Math.round(stats.totalSize / 1024);
              const comment = `## 📦 Bundle Size Analysis\n\n**Total Bundle Size:** ${totalSize}KB\n\n${totalSize > 1024 ? '⚠️ Bundle size exceeds 1MB target' : '✅ Bundle size within limits'}\n\nView detailed analysis in the artifacts.`;
              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: comment
              });
            }

  accessibility-tests:
    runs-on: ubuntu-latest
    needs: frontend-quality
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      - run: npm ci
      - name: Build application
        run: npm run build || npm run -w apps/web build
      - run: npm install -g @axe-core/cli
      - name: Start server
        run: npm start & || npm run -w apps/web start &
        env:
          PORT: 3000
      - run: npx wait-on http://localhost:3000
      - name: Run accessibility tests
        run: |
          axe http://localhost:3000 \
            --tags wcag2a,wcag2aa \
            --exit \
            --save accessibility-results.json
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: accessibility-results
          path: accessibility-results.json
          retention-days: 7

  security-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      - run: npm ci
      - name: Run security audit (high severity threshold)
        run: npm audit --audit-level=high
      - name: Check for known vulnerabilities (non-blocking supplemental)
        run: npx better-npm-audit audit --production || echo "Non-blocking: better-npm-audit reported issues"

  healthcare-compliance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      - run: npm ci
      - name: Check for PII in code (App Router paths)
        run: |
          echo "Checking for potential PII leaks..."
          PATHS=""
          for d in app components lib apps/web/app apps/web/components apps/web/lib; do
            [ -d "$d" ] && PATHS="$PATHS $d"
          done
          if [ -z "$PATHS" ]; then echo "No app paths found"; exit 0; fi
          ! grep -r -i --include="*.ts" --include="*.tsx" --include="*.js" --include="*.jsx" \
            -E "(social.security|ssn|medical.record|patient.id|date.of.birth|phone|email|address|[0-9]{3}-[0-9]{2}-[0-9]{4})" $PATHS || \
          (echo "❌ Potential PII found in code" && exit 1)
          echo "✅ No PII patterns found"
      - name: Validate security headers
        run: |
          echo "Security headers validation would run here"
      - name: Check HIPAA compliance patterns
        run: |
          echo "HIPAA compliance check would run here"